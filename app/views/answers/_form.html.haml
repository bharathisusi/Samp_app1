/ .col-lg-0
/ .col-lg-8.remove-new-answer
/   = form_tag("/questions/#{params[:question_id]}", remote: true, method: :post) do
/     - all_views_side_error_messages!(@answer)
/     .field
/       = label_tag :Answer
/       %br/
/       = text_area_tag "answer[answer]", "", :placeholder => 'Enter your answer', :class =>"form-control", :rows => "8"
/     %br
/     .actions
/       = @answer.id.present? ? (submit_tag "Update Answer", class: " btn btn-primary btn-xs") : (submit_tag "Create Answer", class: " btn btn-primary btn-xs")
/ .col-lg-4
/ .clearfix

