
%h1 Question

- @questions.each do |question|
  - if question.user.present?
    = link_to question.question_box, question
  - if question.user.present?
    - if current_user == question.user
      %span.fullname= link_to 'Edit', edit_question_path(question)

      %span.fullname= link_to 'Destroy', question,method: :delete, data: { confirm: 'Are you sure?' }
  %br
= link_to 'create question', new_question_path,  :class => "btn btn-primary btn-xs"

